import template from './callDelay.html';
import controller from './callDelay.controller';
import './callDelay.scss';

const callDelayComponent = {
    bindings: {},
    template,
    controller,
};

export default callDelayComponent;
